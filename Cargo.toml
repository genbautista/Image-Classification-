[package]
name = "image_classification"
version = "0.1.0"
edition = "2024"

[dependencies]
cifar-ten = { version = "0.5", features = ["download"] }
# Optional: add ndarray for numerical operations
ndarray = "0.15"
